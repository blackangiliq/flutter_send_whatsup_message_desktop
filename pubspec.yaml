name: codecreate_whatsapp_bulk_sender
description: "codecreate whatsApp Bulk Sender"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: '>=3.4.4 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  webview_windows: ^0.4.0
  shared_preferences:
  file_picker: ^8.1.2
  path_provider: ^2.1.4
  http: ^0.13.3
  encrypt: ^5.0.0
  tuple: ^2.0.0 # for Tuple2 class
  crypto: ^3.0.1
  pointycastle: ^3.0.0
  cupertino_icons: ^1.0.6
  win32_registry: ^1.1.4
  win32:
  screenshot: ^3.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

